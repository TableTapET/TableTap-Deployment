name: Continuous Deployment

on:
  push:
    branches: [ "main", "feature/TAB-4-automated-deployment"]

jobs:
  validate-and-prepare-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Deployment Repo
        uses: actions/checkout@v4

      - name: Validate Production Configuration
        run: |
          echo "Validating docker-compose.prod.yml syntax and configuration..."
          docker compose -f docker-compose.prod.yml config

      - name: Dry Run - Simulate Deployment #only for dry run since we dnt have prod server yet
        run: |
          echo "✅ Production configuration is valid."
          echo "⚠️ Actual SSH deployment is paused because production infrastructure is not yet provisioned."
          echo "To enable real deployment, uncomment the SSH step in deploy.yml and add repository secrets."

      # TODO: remebers in the future to uncomment this and fill in with valid vailues once a production server is purchased!
      # - name: Deploy via SSH to Production
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SERVER_IP }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /var/www/tabletap
      #       cd TableTap-Frontend && git pull origin main
      #       cd ../TableTap-Backend && git pull origin main
      #       cd ../TableTap-Deployment && git pull origin main
      #       OCKER_BUILDKIT=1 docker compose -f docker-compose.prod.yml build --no-cache
      #       docker compose -f docker-compose.prod.yml up -d